JWT_SECRET=supersecret
DATABASE_URL="file:./dev.db"
PORT=4001

# App-URL (wird in Mail-Links verwendet)
APP_URL=http://localhost:4200

# Mail (Strato SMTP)
MAIL_HOST=smtp.strato.de
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=
MAIL_PASS=
MAIL_FROM="brand <email>"

# Test-Empfänger für /mail/test
MAIL_TEST_TO=
